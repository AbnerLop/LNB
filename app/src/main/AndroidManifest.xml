<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.itesm.lnb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_full"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_full"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Models.NutriCard"></activity>
        <activity android:name=".Models.IngredientCard"></activity>
        <activity android:name=".Models.QCard"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>